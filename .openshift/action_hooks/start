NOPROGRESS_SETTINGS=production.ini uwsgi -s $OPENSHIFT_PYTHON_IP:$OPENSHIFT_PYTHON_PORT --socket-protocol http --pp $OPENSHIFT_REPO_DIR/noprogress --module wsgi -d $OPENSHIFT_PYTHON_LOG_DIR/appserver.log --pidfile $OPENSHIFT_TMP_DIR/uwsgi.pid
